[package]
name = "cnc"
version = "0.1.0"
edition = "2021"
authors = ["Gustav Berloty <gustav.berloty@epita.fr>"]

[lib]
name = "cnc"
path = "src/lib.rs"

[dependencies]
rand = {version = "0.9.2", optional = true}
arff = "0.3.0"

[features]
random = ["dep:rand"]
